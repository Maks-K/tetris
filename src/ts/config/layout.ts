export const BLOCK_SIDE_LENGTH = 45;
export const ROWS = 20;
export const COLS = 10;

export const infoPanelLayout = {
    WIDTH: 300,
    NEXT: 200,
    LEVEL: 410,
    BURNS: 600,
    TITLE_OFFSET: -25
};

export const layout = {
    width: COLS * BLOCK_SIDE_LENGTH + infoPanelLayout.WIDTH,
    height: ROWS * BLOCK_SIDE_LENGTH
};
